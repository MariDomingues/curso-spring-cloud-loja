eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8080

spring:
  cloud:
    config:
      import-check:
        enabled: false

logstash:
  host: localhost
  port: 5000